# EigenView — Running TODO

Last updated: 2026-04-30 (Bug fixes: layout overlap, factor detail, templates — 247/247 tests)

## Active work queue

| # | Item | Area | Priority | Status | Notes |
|---|------|------|----------|--------|-------|
| 3 | **Signal heat map panel** — top-5 per factor column | Frontend + Backend | **P1** | Approved | New module `signal-heat.js`. `/api/signals/heat` endpoint. Columns: TA / GEX / Flow / Dormant / Sentiment |
| 4 | **`iv_rank` computation during scan** | Backend | **P1** | Pending | Hardcoded `null` in `_pick_to_dict`. Compute from chain ATM IV vs 52-week range at scan time |
| 5 | **Signal freshness badges** — Fresh / Valid / Stale | Backend + Frontend | **P1** | Pending | `signal_fired_at` on Pick, `signal_age_hours` + `price_vs_entry_pct` in API, badge on card |
| 6 | **⭐ Favorites persistence** | Backend + Frontend | **P2** | Pending | Favorites table + `/api/favorites` endpoint |
| 7 | **⟁ Alert wiring** | Backend + Frontend | **P2** | Pending | Define trigger (price enters entry zone) + storage + UI |
| 8 | **Windows Task Scheduler** — 8AM daily scan | Infra | **P2** | Pending | bat/ps1 + Task Scheduler XML |
| 9 | **Category filter** — mylist / closed / alerts nav items | Frontend | **P2** | Pending | Requires #6 and #7 first |
| 10 | **Backend pytest suite updates** | Tests | **P2** | Pending | New pick fields + factor module tests; SignalBench new columns |
| 11 | **DB migration** — apply new SignalBench columns to Supabase | Infra | **P1** | Pending | Added tier, factors_json, direction, setup_type, conviction, entry_low, entry_high, stop |
| 12 | **Backtest chart overlay** — signal markers on price chart | Frontend + Backend | **P3** | Proposed | Toggle btn on chart. `/api/signals/:ticker?tf=1d`. TA backtest feasible now |
| 13 | **Dormant ML training** | Backend | **P3** | Pending | v1 stub only |
| 14 | **Historical options chain data** | Data | **P3** | Blocked | historicaloptiondata.com, needed for GEX/Flow backtest |
| 15 | **Voice orb in default template** | Frontend | **P3** | Pending | Registered, not mounted |

## Completed

| Item | Session | Notes |
|------|---------|-------|
| **Sprint 1: Layout rearrangement** | 2026-04-30 | Nav → horizontal subnav bar. Picks left col (380px). Chart+strip right col (full height). Market context in subnav |
| **Sprint 1: Factor strip detail panels** | 2026-04-30 | Click cell → inline detail (all `factor.detail` keys). Small `?` btn → help overlay. Auto-shows first firing factor |
| **Sprint 2: Tiered signal system** | 2026-04-30 | `tier_score()` in gate.py. Tiers B/C/D saved to SignalBench with full factors_json + all pick fields |
| **Sprint 2: `/api/bench` endpoint** | 2026-04-30 | Returns tiered watch items with tier_label (FORMING/SETUP/UNUSUAL). `/api/bench/count` for badge |
| **Sprint 2: WATCH tab wired** | 2026-04-30 | Category nav WATCH tab → loads bench items via `_loadBench()`. Tier badges on cards |
| **Bug: layout overlap / subnav inflation** | 2026-04-30 | `mountModule` added `.ev-module` (min-height:80px) to 38px subnav slots → inflated and overflowed into body. Fix: skip `.ev-module` class for slot containers. 4 overlap tests added |
| **Bug: factor strip detail empty** | 2026-04-30 | ranker.py missing `detail` in picks `factors_json`. Demo data lacked detail objects. Both fixed |
| **Bug: templates broken (panels always visible)** | 2026-04-30 | `applyTemplate()` only toggled chat. Now hides/shows picks/main/strip/chat slots. CSS `grid-column` explicit placement fixes 0-width column trap |
| **Bug: injectMockPicks missing detail** | 2026-04-30 | Test helper had no detail fields — factor strip always showed empty. Added full detail to all factors |
| **Comprehensive test suite** | 2026-04-30 | 243/243 passing. Added template panel visibility tests (6 tests) + factor detail content tests (3 tests) |
| Pick-cards date navigation (◀ TODAY ▶) | 2026-04-30 | `_loadDates()` + prev/next handlers |
| DEMO button on empty state | 2026-04-30 | `_loadDemo()` injects NVDA/META/TSLA mock picks |
| `/api/picks?date=` + `/api/picks/dates` | 2026-04-30 | Date filtering + available dates endpoint |
| Light/glass/bento theme black panels | 2026-04-29 | CSS var aliases in themes.css |
| Maximize restore collapse bug | 2026-04-29 | Save/restore `_preMaxStyle` in price-chart.js |
| Cards clip content on resize | 2026-04-29 | `min-height: 80px` on `.ev-module` |
| NDX100 real ticker list | 2026-04-29 | `--universe ndx100` flag now works |
| Auto-refresh (5 min poll) | 2026-04-29 | setInterval in app.js |
| Help page module (9 tabs) | 2026-04-29 | `EV_Help.open(tabId)` API, H key shortcut |
| Chip + factor strip → help links | 2026-04-29 | `data-help-tab` + click handlers (now `?` btn on strip) |
| HELP button visible in header | 2026-04-29 | Changed from ghost/invisible to `class="btn"` |
| 233 comprehensive Playwright tests | 2026-04-29 | All passing (pre-Sprint 1) |

## Test policy
- Smoke tests (`tests/ui/dashboard.spec.js`, 22 tests): run after any JS/CSS change
- Comprehensive (`tests/ui/comprehensive.spec.js`, 234 tests): run after major feature or layout changes
- **All 234 comprehensive tests passing** as of 2026-04-30
